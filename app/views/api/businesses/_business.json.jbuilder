json.extract! business, :id, :name, :location, :rating, :phone_number, :website, :open_hours, :num_rating, :categories, :price, :pictures, :zipCode, :city, :lat, :lng
# @businesses.each do |business|
#     # json.set! business.id do
#     #     json.extract! business.reviews[0], :id, :rating, :body, :business_id, :user_id, :date, :first_name, :last_name
#     # end
# end

