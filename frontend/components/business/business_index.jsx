import React from 'react';

class BusinessIndex extends React.Component {
    constructor(props) {
        super(props);
    }
    
    render() {
        return(
            <div>
                {/* {this.props.state.id}
                {this.props.state.name}
                {this.props.state.location}
                {this.props.state.rating}
                {this.props.state.phone_number}
                {this.props.state.website}
                {this.props.state.open_hours}
                {this.props.state.amenities}
                {this.props.state.num_rating}
                {this.props.state.categories} */}
            </div>
        )
    }
}


export default BusinessIndex;